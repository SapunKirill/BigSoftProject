<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="listVacancy">
                <?php
                if (empty($vacancy)) {
                    echo '<div class="alert alert-info">Нет результатов</div>';
                } else {
                    foreach ($vacancy as $vacancyResult) {
                        echo '<div class="panel panel-default">';
                        echo '<div class="panel-heading">                           
                        <h3 class="panel-title">' . $vacancyResult->getName() . '</h3>
                        </div>';
                        echo '<div class="panel-body">' . $vacancyResult->getDescription() . '</div>';
                        echo '<div class="panel-footer"><div class="price">' . $vacancyResult->getPrice() . '</div>';
                        foreach ($company as $companyResult) {
                            switch ($vacancyResult->getId()) {
                                case $companyResult->getVacancy()->getId():
                                    echo '<a class="company"  href="/company/' . $companyResult->getCompany()->getId() . '">' . $companyResult->getCompany()->getName() . '</a>';
                            }
                        }
                        echo '</div>';
                        echo '</div>';
                    }
                };
                ?>
            </div>
        </div>
    </div>
</div>
